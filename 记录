internVLA

问题1：目标点经常被映射到很远的地方
解决用网格最小深度作为z坐标
大区域深度分位数滤波算法

问题2：
模型推理结果不稳定会影响导航效果

问题3：会把红色的东西识别为红色的衣服，但实际不是
解决：云端更严格定位+到达验证+成功前二次验证

没有加入记忆功能v1：
cloud_brain_server.py
local_robot_simple.py

加入记忆功能和FBEv2：
local_robot_simple_v2.py
防止卡在哪里出不来

————————————————————————————FBE——————————————————————————————————————————————
FBE 本身绝对适用于真实未知场景，不需要 .glb。
你的代码现在能跑，是因为 Habitat 仿真器帮你代劳了底层避障工作（借用了 .glb）。
对于目前的中期检查，不要去死磕写真实的 FBE 建图代码，继续用现在的 pathfinder。
你只需要在文档中写清楚：“由于本项目的重点是评估大模型的视觉导航推理能力（System 2），
底层的路径执行（System 1）我们采用了 Habitat 提供的 NavMesh 作为理想的局部规划器替代品”。


加入开始方向判定策略v3：
4个方向的可能性
发现目标后不再触发FBE避免越走越远
避免随意转动视角看不到目标

评估结果分析：
改进1：取消最终成功判定，只要小车在导航过程中的位置距离目标小于1m就视为成功
改进2：

一、待评估：
1.整个系统评估（系统1+系统2）
2.关掉大模型，让机器人瞎走或碰到墙就右转，跑 相同的步数
目的：提供一个下限（Lower Bound），证明测试场景足够复杂，瞎猫碰不上死耗子（SR 极低）。
3.云端挂载 InternVLA-N1-System2模型，本地挂载InternVLA-N1-System1模型，跑相同的步数，对比结果
4.用回 Qwen2.5-VL，但故意关掉你的“31x31大区域深度分位数滤波”，直接取单像素深度作为z坐标，对比结果
目的：证明滤波算法的有效性
5.第一个版本的评估

二、待实现
1.成功案例对比
2.“深度滤波”生效瞬间截图 (The 'Aha' Moment)
3.失败案例分析 (Failure Case Analysis)
